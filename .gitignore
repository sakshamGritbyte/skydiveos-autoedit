sample-data
raw-storage

# Rendered job artifacts (final.mp4 etc). The persisted EDL is the durable
# record; rendered video is a reproducible output, not source. See /render and
# edl.storage for the <jobs_root>/{job_id}/ layout.
jobs

# Downloaded MediaPipe model bundle (cached on demand by /analysis; see
# analysis/models.py). It's a reproducible build artifact, not source.
analysis/models/

# Node deps for the TypeScript web layer (review-ui etc).
node_modules/
package-lock.json

# Python bytecode caches and macOS Finder cruft — build/OS artifacts, not source.
__pycache__/
*.pyc
.DS_Store
